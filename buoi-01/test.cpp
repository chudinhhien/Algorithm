#include<bits/stdc++.h>
using namespace std;

using ll = long long;

int main() {
  int n; cin >> n;
  vector<int> v(n); //rỗng
  for(int i = 0 ; i < n ; i++) {
    int tmp; cin >> tmp;
  }
  for(int i = 0 ; i < n ; i++) {
    cout << v[i] << ' ';
  }
  return 0;
}
